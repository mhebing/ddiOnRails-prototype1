<table>
  <tr>
    <th>Name</th>
    <th>Label</th>
  </tr>

<% @studies.each do |study| %>
  <tr>
    <td><%= link_to study.name, study %></td>
    <td><%= study.label %></td>
  </tr>
<% end %>
</table>

