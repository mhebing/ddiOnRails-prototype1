<div class="span-24">
<h1><%= @basket.title %>
</div>

<hr>

<div class="span-8">
<div class="box">
<%= render 'info' %>
</div>

<% if can? :manage, @basket %>
<%= link_to 'Edit', edit_basket_path(@basket) %> |
<%= link_to 'Back', baskets_path %>
<% end %>
</div>

<div class="span-16 last">
<h2>Variable Groups in <%= @basket.title %></h2>
<%= render 'variable_groups/table' %>
</div>
