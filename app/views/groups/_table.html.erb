<table>
  <tr>
    <th>Name</th>
    <th>Label</th>
    <th>Study</th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= link_to group.name, group %></td>
    <td><%= group.label %></td>
    <td><%= link_to group.study.name,
            group.study unless
            group.study.blank? %></td>
  </tr>
<% end %>
</table>

