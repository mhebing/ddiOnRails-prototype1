<%= form_for(@logical_product) do |f| %>
  <% if @logical_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@logical_product.errors.count, "error") %> prohibited this logical_product from being saved:</h2>

      <ul>
      <% @logical_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :label %><br />
    <%= f.text_field :label %>
  </div>
  <div class="field">
    <%= f.label :abstract %><br />
    <%= f.text_area :abstract %>
  </div>
  <div class="field">
    <%= f.label :study_unit_id %><br />
    <%= f.number_field :study_unit_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
